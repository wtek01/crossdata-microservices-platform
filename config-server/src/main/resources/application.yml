server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/wtek01/crossdata-config-repo.git
          default-label: main
          username: wtek01
          password: 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
          # Use your GitHub PAT here
          # If using SSH, configure private key access instead

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"
